# Client-side API (baked in at build time). For local dev, you can instead set
# API_URL and API_KEY in the repository root .env — vite.config.ts maps them to
# PUBLIC_* automatically.
PUBLIC_API_URL=
PUBLIC_API_KEY=
